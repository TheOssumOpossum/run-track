import React from 'react';
import RacesScreen from '../../RacesScreen';

// Wrapper to place profile-related screens under src/screens/profile
export default function Races(props) {
  return <RacesScreen {...props} />;
}
